/*
 * The MyStringTokenizer class reads successive tokens from
 * the provided string, one at a time
 */

import acm.program.*;

public class MyStringTokenizer {
	
	// Private instance variable
	private String tokenString;     // The provided string
	
	/**
	 * Creates a new String Tokenizer object that reads
	 * whitespace-separated tokens from str.
	 * @param str The provided text as a String
	 */
	public MyStringTokenizer(String str) {
		tokenString = str;
	}
	
	/**
	 * Creates a StringTokenizer that uses the characters in
	 * in delims as token separators.
	 * @param str
	 * @param delims
	 */
	
}
